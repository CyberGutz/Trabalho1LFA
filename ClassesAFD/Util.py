def char_remove(old, to_remove):
        nova_string = old
        for x in to_remove:
            nova_string = nova_string.replace(x, '')

        return nova_string


def copiaAfd(afd):
        copia = afd;
        return copia;

    